# Конфиг для Amvera Cloud: деплой из репозитория, без терминала.
# Постоянные данные лежат в /data, https-домен выдаётся автоматически.
meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: Dockerfile
run:
  persistenceMount: /data
  containerPort: 8080
