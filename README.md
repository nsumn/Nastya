# Telegram-бот приёма оплаты (Platega / Карта РФ / Telegram Stars)

Бот для продажи доступа к тарифам с тремя способами оплаты:

| Способ | Как работает |
|--------|--------------|
| 📲 **СБП** | Через платёжную систему **Platega.io**. Бот создаёт счёт, пользователь оплачивает, **бот автоматически проверяет оплату** (вебхук + резервный опрос статуса) и сам выдаёт ссылку на канал. |
| 💳 **Карта РФ** | Ручной режим: бот показывает реквизиты, пользователь оплачивает и присылает чек. **Чек прилетает администратору в личку**, администратор проверяет вручную и **переписывается с покупателем прямо в чате бота**, затем присылает ссылку на канал. |
| ⭐ **Telegram Stars** | Просто ссылка на другой канал. |

## Сценарий

1. `/start` → приветствие + кнопка тарифа (**VIP ОГЭ 2026**).
2. Кнопка тарифа → карточка тарифа (название, цена `1590.0 RUB`, срок «навсегда», описание) + кнопка **Оплатить**.
3. **Оплатить** → выбор способа: СБП / Карта РФ / Telegram Stars.
4. Дальше — по таблице выше.

## Установка

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

cp .env.example .env   # заполни своими значениями
python -m bot.main
```

## Настройка (`.env`)

Главные параметры (полный список с комментариями — в `.env.example`):

- `BOT_TOKEN` — токен от [@BotFather](https://t.me/BotFather).
- `ADMIN_CHAT_ID` — твой `chat_id` (узнать у [@userinfobot](https://t.me/userinfobot)). Сюда приходят чеки и здесь ты переписываешься с покупателями.
- `PLATEGA_MERCHANT_ID`, `PLATEGA_SECRET` — данные из ЛК Platega.
- `PLATEGA_SBP_METHOD` — метод оплаты в Platega (по умолчанию `2` = СБП).
- `PUBLIC_BASE_URL` — публичный адрес бота для вебхука Platega.
- `CHANNEL_LINK` — ссылка на канал, которую бот выдаёт после оплаты по СБП.
- `STARS_LINK` — ссылка для оплаты Telegram Stars.
- `CARD_DETAILS` — реквизиты для оплаты картой РФ.
- `REVIEWS_LINK`, `ADMIN_LINK` — ссылки для кнопок «Отзывы» и «Администрация» в приветствии (необязательно).

### Подключение вебхука Platega (для авто-проверки СБП)

Бот поднимает HTTP-сервер на `PORT` (по умолчанию `8080`) с эндпоинтом:

```
POST <PUBLIC_BASE_URL>/platega/callback
```

Укажи этот URL в личном кабинете Platega как адрес callback. Бот проверяет
заголовки `X-MerchantId` / `X-Secret` и отвечает `200 OK`.

> Если вебхук не настроен или не дошёл — оплата всё равно подтвердится:
> после создания счёта бот **сам опрашивает статус** транзакции (polling),
> а у пользователя есть кнопка «🔄 Проверить оплату».

## Как переписываться с покупателем (Карта РФ)

1. Пользователь жмёт «✅ Оплатить и отправить чек» и присылает чек.
2. Тебе в ЛС приходит уведомление и сам чек.
3. **Ответь reply** на сообщение пользователя — твой ответ придёт ему в чат бота
   (так можно писать «не вижу чека», задавать вопросы и т. д.).
4. Когда оплата подтверждена — просто **отправь ему reply со ссылкой на канал**.

## Добавить новый тариф

Открой `bot/config.py`, скопируй блок `Tariff(...)` в функции `load_config`,
поменяй `id`, `button`, `title`, `price`, `description` и добавь в словарь
`tariffs`. Кнопки в приветствии построятся автоматически.

## Структура

```
bot/
  config.py      — конфигурация и тарифы
  texts.py       — тексты сообщений
  keyboards.py   — инлайн-клавиатуры
  database.py    — SQLite: заказы и карта переписки
  platega.py     — клиент Platega.io API
  services.py    — выдача доступа и опрос статуса
  webhook.py     — приём вебхуков Platega (aiohttp)
  roblox.py      — клиент публичного API Roblox (возраст аккаунта)
  op.py          — обязательная подписка: разбор списка и проверка
  op_store.py    — общий файл состояния ОП (один на все боты)
  webapp.py      — HTTP мини-приложения: /app и /api/roblox/user
  miniapp/
    index.html   — сама страница мини-аппа (Telegram WebApp)
  handlers/
    start.py     — /start и навигация по тарифам
    payment.py   — способы оплаты (СБП / карта / stars)
    roblox.py    — /roblox <ник> и кнопка запуска мини-аппа
    op_admin.py  — приём дневного списка ссылок и проверочной ссылки
    relay.py     — двусторонний чат покупатель ⇄ администратор
  main.py        — точка входа
panel/
  main.py        — бот-админпанель: меняет ссылки сразу во всех ботах
```

## Режимы бота

`BOT_MODE` в `.env` определяет, что показывает бот:

| Режим | Что внутри |
|-------|------------|
| `payments` (по умолчанию) | продажа тарифов ОГЭ: витрина, оплаты, переписка с покупателем |
| `roblox` | только проверка возраста аккаунта Roblox + обязательная подписка |

В режиме `roblox` человеку достаточно прислать ник сообщением, а нижнее меню
администратора состоит из одной кнопки «📢 Спонсоры (ОП)».

## Мини-приложение «Возраст аккаунта Roblox»

Пользователь вводит ник Roblox — бот показывает дату регистрации, возраст
аккаунта (годы/месяцы/дни), аватар, ID, число подписчиков и метки
verified / бан. Данные берутся из **публичного API Roblox** (ключи не нужны).

Два способа использования:

| Где | Как включить |
|-----|--------------|
| **Мини-приложение (WebApp)** | Нужен https-адрес: задай `PUBLIC_BASE_URL=https://твой-домен` (или отдельно `MINIAPP_URL`). Страница отдаётся ботом по `/app`, кнопка «🎮 Возраст Roblox» появляется в нижнем меню и открывает её внутри Telegram. |
| **Команда в чате** | `/roblox builderman` — работает всегда, https не нужен. |

Принимается ник, `@ник`, числовой ID или ссылка вида
`https://www.roblox.com/users/156/profile`.

Как это устроено:

1. `POST users.roblox.com/v1/usernames/users` — ник → `userId`.
2. `GET users.roblox.com/v1/users/{id}` — поле `created` (дата регистрации).
3. `thumbnails` / `friends` — аватар и счётчики.

Запросы идут через сервер бота (не из браузера), ответы кэшируются на 5 минут,
на пользователя действует лимит 20 запросов в минуту. API мини-аппа проверяет
подпись Telegram `initData` (HMAC от токена бота) — со стороннего сайта
дёрнуть его не выйдет. Для отладки в обычном браузере поставь
`MINIAPP_ALLOW_ANON=1`.

Чтобы мини-апп открывался ещё и кнопкой рядом с полем ввода: BotFather →
`/mybots` → бот → *Bot Settings* → *Menu Button* → указать `https://твой-домен/app`.

## Обязательная подписка на спонсоров (ОП)

Пока человек не подписан на **проверочный канал**, проверка ников Roblox ему не
отвечает: вместо результата бот показывает список каналов-спонсоров и кнопку
«✅ Я подписался». Нажал — бот перепроверяет подписку и сразу выдаёт то, что
человек искал.

### Ежедневная замена спонсоров

Ничего настраивать руками не нужно — бот разбирает сообщение сам:

1. Присылаешь боту дневное сообщение со ссылками (то, что приходит от
   координатора ОП). Бот вытаскивает все ссылки **в исходном порядке**,
   выбрасывает служебные строки и пометки вроде «(старт)», «(добавь группу)»
   и запоминает список. Строка «Твоя проверочная ссылка» сохраняется как место
   под проверочную ссылку.
2. Следующим сообщением присылаешь новую проверочную ссылку — она встаёт
   ровно на это место, под названием проверочного канала.
3. Бот сразу показывает, как список выглядит для людей.

`/op` — что стоит сейчас: сколько ссылок, за какое число («отметка»), когда
обновлено, какой канал проверяется, включена ли проверка.

### Один список на несколько ботов

Состояние ОП (список, проверочная ссылка, проверочный канал) лежит в общем
JSON-файле — путь задаётся `OP_STATE_FILE`. Все боты на сервере, у которых
указан **один и тот же файл**, показывают один список: поменял в одном месте —
изменилось везде, перезапускать ничего не нужно.

Для этого есть отдельный бот-панель (`python -m panel.main`, свой токен в
`PANEL_BOT_TOKEN`):

- **🔄 Поменять все ссылки** — присылаешь дневное сообщение, затем новую
  проверочную ссылку; список встаёт сразу во все боты;
- **🤖 Мои боты** — какие боты подключены к общему списку и может ли каждый
  из них проверять подписку (✅ — бот админ в проверочном канале);
- **👀 Как видят люди** — предпросмотр;
- кнопка включения/выключения проверки.

Каждый бот при запуске отмечается в этом файле сам.

### Проверочный канал

Подписка проверяется **только по нему** — Telegram позволяет проверить членство
лишь там, где бот администратор. Бот запоминает канал сам, когда его делают
админом; можно и вручную — переслать боту любой пост из этого канала.

Если канал не задан или Telegram не отвечает, бот никого не блокирует
(в лог пишется предупреждение) — люди не застревают из-за настройки.
Подписка кэшируется на 10 минут.

## Технологии

- [aiogram 3](https://docs.aiogram.dev/) — Telegram Bot API
- aiohttp — приём вебхуков
- aiosqlite — хранилище заказов и переписки
- Telegram Mini Apps (WebApp) + публичный API Roblox
